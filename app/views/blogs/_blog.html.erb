<div id="<%= dom_id blog %>">
  <p>
    <strong>Title:</strong>
    <%= blog.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= blog.body %>
  </p>


  <p>
    <strong>Picture:</strong>
    <%= image_tag(blog.picture_url, width: 600) if blog.picture.present? %>
  </p>

</div>
